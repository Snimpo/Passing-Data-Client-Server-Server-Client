<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Name Letters</title>
  </head>

  <body>
    <!-- Here we need to add an if conditional to render the correct H1 if the user has not written anything yet-->
    <% if (locals.nLetters) { %>
    <h1>The number of letters on your name is: <%= nLetters %></h1>
    <% } else { %>
    <h1>Please insert your name and last Name</h1>
    <% } %>
    
    <form action="/submit" method="POST">
      <input type="text" name="fName" placeholder="First name" />
      <input type="text" name="lName" placeholder="Last name" />
      <input type="submit" value="OK" />
    </form>
  </body>
</html>
